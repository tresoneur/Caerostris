<div class="d-flex flex-row pb-4 mb-4 justify-content-start align-items-center">

    <img src=@ImageUri class="mr-4 context-page-heading-image" />

    <div class="d-flex flex-column justify-content-center align-items-start h-100">

        <p class="context-page-heading-type">@Type</p>

        <h4 class="context-page-heading-title">@((MarkupString)Title)</h4>

        @if (!(By is null))
        {
            <div><span class="context-page-heading-by">By</span> @((MarkupString)By)</div>
        }

        <div class="context-page-heading-childcontent">
            @ChildContent
        </div>

    </div>

</div>

@code
{
    [Parameter]
    public string Type { get; set; }

    [Parameter]
    public string ImageUri { get; set; }

    [Parameter]
    public string Title { get; set; }

    [Parameter]
    public string? By { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}
